# UAV Multi Relay

This project researches multi-relay, single-task UAV communication.

The logical topology is `H -> R1 -> ... -> RK -> L`.

The project has currently completed only its initial setup.

## Installation

```bash
python -m pip install -e ".[dev]"
```

## Tests

```bash
pytest
```
